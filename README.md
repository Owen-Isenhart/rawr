user has their model that they send into battle like a pokemon

we can have a default model like dolphin but allow users to pull in their own from ollama and give custom context/instructions to their model

